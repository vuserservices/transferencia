dentro de lo que hay que hacer ahora es:
el archivo BBDD solo tomaremos lo != a 2024, donde hay que fijarse en que el contrato, solicitud, y estado (Documentacion) exista como estado final
es decir, firmado, en caso que diga firmado, no hacer nada y solo seguir con el siguiente.

en el archivo sgt, solo tomaremos los valores 2024 igualmente para agregarlos, inicialmente revisar si existe dentro de BBDD, sino se añade. incluso, lo que har+ia sería procesarlo dentro de su propio archivo 
para que el dfbbdd no se afecte con las formulas y cuando esté OK se concatena BBDD+informeSGT en ese orden.
obviamente la concatenacion serán las lineas nuevas procesadas bajo las formulas que ya tenemos, cruce con SOlicitudes, con peduidos, con contratos, sourcing y RC.

para las lineas de bbbdd que no tengan datgos igualmente tendría que ser procesados, de hecho el filtro debiese ser un df sin procesar de ddbb en blancos y los sgt sin procesar y
solo a esa tabla aplicarle las transformaciones.

result = collection_ip.update_one(
                {"rpa_name": 'scrapingE1D', "Date": '10/06/2024',"time":'09:30','Valid':"True"},
                {"$inc": {"count_executions": 1}},
                upsert=True
            )
